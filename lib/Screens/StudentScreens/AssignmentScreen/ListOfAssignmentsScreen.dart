import 'package:flutter/material.dart';

class ListOfAssignmentsScreen extends StatelessWidget {
  const ListOfAssignmentsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return SingleChildScrollView(
      child: Center(
        child: Column(
          children: [],
        ),
      ),
    );
  }
}
